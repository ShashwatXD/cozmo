[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cozmo"
version = "0.2.0"
description = "Production-style CLI coding agent: ReAct tools, RAG, memory, eval, OpenAI/Ollama gateway"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.11"
authors = [
    { name = "Shashwat Pratap Singh", email = "singhshashwat.go@gmail.com" },
]
dependencies = [
    "typer>=0.12",
    "pydantic-settings>=2.0",
    "python-dotenv>=1.0",
    "openai>=1.40",
    "tenacity>=8.2",
    "questionary>=2.0",
    "rich>=13.0",
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[project.scripts]
cozmo = "cozmo.cli.main:app"

[tool.hatch.build.targets.wheel]
packages = ["src/cozmo"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
